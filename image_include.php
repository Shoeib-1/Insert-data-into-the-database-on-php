
// if (isset($_FILES['image'])){

    // $errors = array();
    // $extensions = array("png","jpeg","jpg");
    // $image_name = $_FILES['image']['name'];
    // $image_tmp = $_FILES['image']['tmp_name'];
    // $image_size = $_FILES['image']['size'];
    // $image_type = $_FILES['image']['type'];
    // $image_extension = strtolower(end(explode('.',$image_name)));


    // if (in_array($image_extension,$extensions) === false){
    //     $errors[] = "image type is not allowed!";
    // }

    // if ($image_size > 297152){
    //     $errors[] = "image size is not allowed!";
    // }


    // if (empty($errors)){
    //     move_uploaded_file($image_tmp,"uploads"."/".$image_name);
    // }else{
    //     foreach ($errors as $error){
    //         echo $error."<br>";
    //     }
    // }

// }


